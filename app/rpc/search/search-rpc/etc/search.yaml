Name: search.rpc
ListenOn: "${SEARCH_RPC_LISTEN_ON}"

Telemetry:
  Name: search-rpc
  Disabled: ${OTEL_DISABLED}
  Endpoint: "${OTEL_ENDPOINT}"
  Batcher: otlpgrpc
  Sampler: 1.0

Prometheus:
  Host: "${PROM_HOST}"
  Port: ${SEARCH_PROM_PORT}
  Path: /metrics

Log:
  ServiceName: "search-rpc"
  Mode: "file"
  Encoding: "json"
  TimeFormat: "2006-01-02T15:04:05.000Z07:00"
  Path: "${LOG_PATH}/search-rpc"
  Level: "info"
  MaxContentLength: 0
  Compress: false
  Stat: true
  KeepDays: 1
  StackCooldownMillis: 100
  MaxBackups: 0
  MaxSize: 0
  Rotation: "daily"
  FileTimeFormat: "2006-01-02T15:04:05.000Z07:00"

Etcd:
  Hosts:
    - "${ETCD_HOST}:${ETCD_PORT}"
  Key: search.rpc

InteractionRpcClientConf:
  Etcd:
    Hosts:
      - "${ETCD_HOST}:${ETCD_PORT}"
    Key: interaction.rpc

RedisConfig:
  Host: "${REDIS_HOST}:${REDIS_PORT}"
  Type: node
  Pass: ""

SearchCacheEnabled: ${SEARCH_CACHE_ENABLED}
SearchSnapshotEnabled: ${SEARCH_SNAPSHOT_ENABLED}
SearchHybridRankEnabled: ${SEARCH_HYBRID_RANK_ENABLED}
SearchBackend: "${SEARCH_BACKEND}"
SearchEngineTrafficPercent: ${SEARCH_ENGINE_TRAFFIC_PERCENT}
SearchEngineCompareEnabled: ${SEARCH_INDEX_COMPARE_ENABLED}
SearchEngine:
  Endpoint: "${SEARCH_INDEX_ENGINE_ENDPOINT}"
  ContentIndex: "${SEARCH_INDEX_CONTENT_INDEX}"
  UserIndex: "${SEARCH_INDEX_USER_INDEX}"
  Username: "${SEARCH_INDEX_ENGINE_USERNAME}"
  Password: "${SEARCH_INDEX_ENGINE_PASSWORD}"
  TimeoutMs: ${SEARCH_INDEX_ENGINE_TIMEOUT_MS}
SearchSnapshotTTLSeconds: ${SEARCH_SNAPSHOT_TTL_SECONDS}
SearchSnapshotMaxItems: ${SEARCH_SNAPSHOT_MAX_ITEMS}
SearchQueryCacheTTLSeconds: ${SEARCH_QUERY_CACHE_TTL_SECONDS}
SearchDocCacheTTLSeconds: ${SEARCH_DOC_CACHE_TTL_SECONDS}
SearchQueryCacheMaxPages: ${SEARCH_QUERY_CACHE_MAX_PAGES}

MySQL:
  DataSource: "${MYSQL_USER}:${MYSQL_PASSWORD}@tcp(${MYSQL_HOST}:${MYSQL_APP_PORT})/zfeed?charset=utf8mb4&parseTime=true&loc=Asia%2FShanghai"
